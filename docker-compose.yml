services:
  backend:
    build: ./backend
    container_name: backend-container
    ports:
      - "8000:8000"
    restart: always

  frontend:
    build: ./frontend
    container_name: frontend-container
    ports:
      - "3000:3000"
    restart: always
    depends_on:
      - backend
